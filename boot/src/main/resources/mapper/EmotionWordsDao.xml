<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.dao.EmotionWordsDao">

    <resultMap type="com.boot.entity.EmotionWords" id="EmotionWordsMap">
        <result property="emoId" column="emo_id" jdbcType="INTEGER"/>
        <result property="engContent" column="eng_content" jdbcType="VARCHAR"/>
        <result property="cnContent" column="cn_content" jdbcType="VARCHAR"/>
        <result property="orderId" column="order_id" jdbcType="INTEGER"/>
        <result property="emoCreateTime" column="emo_create_time" jdbcType="TIMESTAMP"/>
        <result property="emoAuthor" column="emo_author" jdbcType="VARCHAR"/>
        <result property="frequency" column="frequency" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="emoId" useGeneratedKeys="true">
        insert into word.emotion_words(eng_content, cn_content, order_id, emo_create_time, emo_author, frequency)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.engContent}, #{entity.cnContent}, #{entity.orderId}, #{entity.emoCreateTime}, #{entity.emoAuthor},
            #{entity.frequency})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="emoId" useGeneratedKeys="true">
        insert into word.emotion_words(eng_content, cn_content, order_id, emo_create_time, emo_author, frequency)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.engContent}, #{entity.cnContent}, #{entity.orderId}, #{entity.emoCreateTime}, #{entity.emoAuthor},
            #{entity.frequency})
        </foreach>
        on duplicate key update
        eng_content = values(eng_content) , cn_content = values(cn_content) , order_id = values(order_id) ,
        emo_create_time = values(emo_create_time) , emo_author = values(emo_author) , frequency = values(frequency)
    </insert>

</mapper>

