<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.dao.LoginLogDao">

    <resultMap type="com.boot.entity.LoginLog" id="LoginLogMap">
        <result property="logId" column="log_id" jdbcType="INTEGER"/>
        <result property="loginTime" column="login_time" jdbcType="TIMESTAMP"/>
        <result property="account" column="account" jdbcType="VARCHAR"/>
        <result property="ip" column="ip" jdbcType="VARCHAR"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="result" column="result" jdbcType="VARCHAR"/>
        <result property="nickName" column="nick_name" jdbcType="VARCHAR"/>
        <result property="tel" column="tel" jdbcType="VARCHAR"/>
        <result property="remark" column="log_remark" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="logId" useGeneratedKeys="true">
        insert into word.login_log(login_time, account, ip, user_id, result, nick_name, tel, log_remark)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.loginTime}, #{entity.account}, #{entity.ip}, #{entity.userId}, #{entity.result},
            #{entity.nickName}, #{entity.tel}, #{entity.remark})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="logId" useGeneratedKeys="true">
        insert into word.login_log(login_time, account, ip, user_id, result, nick_name, tel, log_remark)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.loginTime}, #{entity.account}, #{entity.ip}, #{entity.userId}, #{entity.result},
            #{entity.nickName}, #{entity.tel}, #{entity.remark})
        </foreach>
        on duplicate key update
        login_time = values(login_time) , account = values(account) , ip = values(ip) , user_id = values(user_id) ,
        result = values(result) , nick_name = values(nick_name) , tel = values(tel) , log_remark = values(log_remark)
    </insert>

</mapper>

