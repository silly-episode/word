<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.dao.CommunityDao">

    <resultMap type="com.boot.entity.Community" id="CommunityMap">
        <result property="communityId" column="community_id" jdbcType="INTEGER"/>
        <result property="version" column="version" jdbcType="VARCHAR"/>
        <result property="wish" column="wish" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="memberNum" column="member_num" jdbcType="INTEGER"/>
        <result property="topOnlineNum" column="top_online_num" jdbcType="INTEGER"/>
        <result property="articleNum" column="article_num" jdbcType="INTEGER"/>
        <result property="moduleNum" column="module_num" jdbcType="INTEGER"/>
        <result property="gitHubAddress" column="git_hub_address" jdbcType="VARCHAR"/>
        <result property="tel" column="tel" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="" useGeneratedKeys="true">
        insert into word.community(community_id, version, wish, create_time, member_num, top_online_num, article_num,
        module_num, git_hub_address, tel)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.communityId}, #{entity.version}, #{entity.wish}, #{entity.createTime}, #{entity.memberNum},
            #{entity.topOnlineNum}, #{entity.articleNum}, #{entity.moduleNum}, #{entity.gitHubAddress}, #{entity.tel})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="" useGeneratedKeys="true">
        insert into word.community(community_id, version, wish, create_time, member_num, top_online_num, article_num,
        module_num, git_hub_address, tel)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.communityId}, #{entity.version}, #{entity.wish}, #{entity.createTime}, #{entity.memberNum},
            #{entity.topOnlineNum}, #{entity.articleNum}, #{entity.moduleNum}, #{entity.gitHubAddress}, #{entity.tel})
        </foreach>
        on duplicate key update
        community_id = values(community_id) , version = values(version) , wish = values(wish) , create_time =
        values(create_time) , member_num = values(member_num) , top_online_num = values(top_online_num) , article_num =
        values(article_num) , module_num = values(module_num) , git_hub_address = values(git_hub_address) , tel =
        values(tel)
    </insert>

</mapper>

